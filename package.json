{
  "name": "get-icon-path",
  "version": "0.0.1",
  "description": "Get the icon path from the icon theme,  for KDE, GNOME, in accordance to the freedesktop standards",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Maxerbox",
  "license": "ISC",
  "dependencies": {
    "node-gsettings-wrapper": "^0.5.0",
    "xdg-basedir": "^3.0.0",
    "xdg-parse": "0.0.2"
  }
}
